# Database configuration
POSTGRES_DB=door_workflow
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_strong_postgres_password_here

# JWT Security configuration
# Note: In production, generate a secure random 256-bit+ secret key
JWT_SECRET=your_production_jwt_secret_must_be_long_and_secure_32bytes
JWT_EXPIRATION=86400
